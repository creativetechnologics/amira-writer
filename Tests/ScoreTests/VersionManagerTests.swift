import Testing
@testable import ScoreUI

@available(macOS 26.0, *)
@Suite struct VersionManagerTests {
    // Phase 4A: VersionManager smoke test
    // Full behavior testing requires a hydrated ScoreStore — flagged for future session.
}
